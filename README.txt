Program prompts user for two integer values and number of bits desired for result by user

The signed binary representation of each number is displayed 

The signed binary sum is also displayed if value fits user selected bit size, otherwise overflow error displayed